# Uses the same paired loader as RGBT-3M, with one fire class.
name: RGBT-3M
path: /path/to/RGB-T-Wildfire
train: train.txt
val: val.txt
nc: 1
names: {0: fire}
input_channels: 6
input_mode: dual_input
rgb_dir: RGB
thermal_dir: IR
label_dir: labels
image_ext: .png
img_size: [512, 640]
format: yolo
